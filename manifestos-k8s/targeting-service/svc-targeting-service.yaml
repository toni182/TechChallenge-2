apiVersion: v1
kind: Service
metadata:
  name: targeting-service-svc
  namespace: default
spec:
  selector:
    app: targeting-service
  ports:
  - name: http
    port: 80
    targetPort: 8002