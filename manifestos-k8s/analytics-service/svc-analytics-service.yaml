apiVersion: v1
kind: Service
metadata:
  name: analytics-service-svc
  namespace: default
spec:
  selector:
    app: analytics-service
  ports:
  - name: http
    port: 80
    targetPort: 8005